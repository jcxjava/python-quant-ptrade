# PTrade 量化策略编码规范

> 本文档是项目级编码规范的**唯一维护源**，适用于 Claude Code 和 OpenCode。
> 策略最终运行在国金 PTrade 平台，所有代码必须严格遵循以下规范。

---

## 1. 运行环境约束

### 1.1 Python 版本

- **目标运行环境**：国金 PTrade Python 3.11
- **本地开发环境**：Anaconda Python 3.12
- **兼容要求**：代码必须向下兼容 Python 3.11，不使用 3.12+ 独占语法

### 1.2 依赖约束

| 规则 | 说明 |
|------|------|
| **仅允许导入** | Python 标准库、`numpy`、`pandas`、`scipy`、`statsmodels`、`scikit-learn` |
| **禁止导入** | `requests`、`tushare`、`akshare`、`torch`、`tensorflow` 等网络/深度学习库 |
| **禁止 pip install** | 策略代码中不得包含任何 pip 安装命令，PTrade 平台无法安装第三方包 |
| **禁止外网请求** | 运行环境无外网访问权限，禁止编写任何外部网络请求逻辑 |

### 1.3 API 约束

> PTrade API 完整文档：https://ptradeapi.com/

- **必须使用** PTrade 原生 API（如 `get_history`、`get_ashares`、`order_target_value` 等）
- **禁止使用** `《PTrade 所有 API 函数接口清单》` 中未列出的函数
- PTrade API 由平台全局注入，**无需 import**

```python
# ✅ 正确：使用 PTrade 原生 API
df = get_history(10, '1d', 'close', security, fq=None, include=False)
position = get_position(security)
order_value(security, cash)

# ❌ 错误：使用未在清单中的函数 / 外部网络请求
import requests                     # 禁止
data = tushare.get_k_data('600570') # tushare 未在清单中
df = get_stock_data(security)       # 不存在于 PTrade API 清单
```

---

## 2. 策略生命周期

PTrade 量化引擎以事件触发为基础，严格遵循以下生命周期：

```
initialize → before_trading_start → handle_data → after_trading_end
```

### 2.1 `initialize(context)` — 必选

初始化全局变量和策略参数，**每个回测周期开始时运行一次**。

```python
def initialize(context):
    # 设置股票池
    g.security = '600570.SS'
    set_universe(g.security)

    # 自定义参数
    g.ma_short = 5
    g.ma_long = 20
    g.stop_loss = -0.05

    # 回测配置
    set_benchmark('000300.XBHS')
    set_commission(commission_ratio=0.0003, min_commission=5.0)
    set_slippage(slippage=0.002)
```

### 2.2 `before_trading_start(context, data)` — 可选

盘前处理，**每交易日 9:10 运行一次**。用于过滤停牌/ST 股票、更新当日股票池。

### 2.3 `handle_data(context, data)` — 必选

盘中主逻辑，**执行时机取决于策略周期**：

| 周期 | 运行方式 | 默认时间 |
|------|---------|---------|
| **日线** | 每日执行一次 | 14:50 |
| **分钟** | 每分钟执行一次 | 09:30 ~ 15:00 |
| **Tick** | 见下方说明 | — |

#### 分钟策略筛选示例

```python
def handle_data(context, data):
    current_time = context.blotter.current_dt.time()

    # 每隔 5 分钟执行一次
    if current_time.minute % 5 != 0:
        return

    # 固定时间执行（如 9:31）
    if current_time.hour != 9 or current_time.minute != 31:
        return
```

### 2.4 `after_trading_end(context, data)` — 可选

盘后处理，**每交易日 15:30 运行一次**。用于输出绩效统计、记录日志。

### 2.5 Tick 级别（可选）

- `tick_data(context, data)`：每 3 秒执行一次（固定间隔）
- `run_interval(context, func, seconds=10)`：自定义秒数间隔（最小 3 秒）
- **二者选其一，不可同时使用**

---

## 3. 文件结构规范

### 3.1 策略文件要求

- 策略代码是**独立的 `.py` 文件**，可直接复制到 PTrade 编辑器运行
- 文件首条语句为**模块文档字符串**（`"""..."""`），简要说明策略用途
- **PTrade 原生 API**（`get_history`、`order_value`、`set_universe` 等）由平台全局注入，**无需 import**
- **允许 import**：Python 标准库、`numpy`、`pandas`、`scipy`、`statsmodels`、`scikit-learn`
- **禁止 import**：`requests`、`tushare`、`akshare`、`torch`、`tensorflow` 等网络/深度学习库

```python
# ✅ 正确：PTrade 常见导入示例
import pandas as pd
import numpy as np
import statsmodels.api as sm
import math
from decimal import Decimal
import datetime
from datetime import date as justdate
from scipy.optimize import minimize

# PTrade 原生 API 无需 import，直接调用
def initialize(context):
    g.security = '600570.SS'
    set_universe(g.security)

def handle_data(context, data):
    df = get_history(10, '1d', 'close', g.security, fq=None, include=False)
    price = data[g.security]['close']
    order_value(g.security, context.portfolio.cash)

def after_trading_end(context, data):
    pass

# ❌ 错误：禁止的导入
import requests                     # 禁止，网络库
import tushare as ts                # 禁止，未在允许列表中
import torch                        # 禁止，深度学习库
import tensorflow                   # 禁止，深度学习库
from sklearn.linear_model import LinearRegression  # 禁止，sklearn 的 import 应在前端标记
```

### 3.2 目录约定

```
strategies/
├── _templates/          # _ 前缀 = 模板文件，不直接运行
│   ├── daily.py         # 日线模板
│   ├── minute.py        # 分钟模板
│   └── tick.py          # Tick 模板
├── ma_cross_daily.py    # 策略文件平铺，可直接复制到 PTrade
└── ma_cross_minute.py
```

---

## 4. 命名规范

### 4.1 全局参数（`g` 对象）

使用 `g.小写下划线` 命名，所有可调参数集中在 `initialize()` 中声明。

```python
def initialize(context):
    # ✅ 正确：清晰的命名
    g.ma_short = 5
    g.ma_long = 20
    g.stop_loss = -0.05
    g.take_profit = 0.10
    g.bar_period = 5

    # ❌ 错误：难以理解的命名
    g.a = 5
    g.aa = 20
    g.flag1 = True
```

### 4.2 局部变量

| 含义 | 推荐命名 | 示例 |
|------|---------|------|
| 股票代码 | `security` / `security_list` | `security = g.security` |
| 行情 DataFrame | `df` | `df = get_history(10, '1d', 'close', security)` |
| 收盘价序列 | `close_series` | `close_series = df['close']` |
| 价格 | `price` / `close_price` | `price = data[security]['close']` |
| 持仓对象 | `position` / `positions` | `position = get_position(security)` |
| 可用资金 | `cash` | `cash = context.portfolio.cash` |
| 总资产 | `portfolio_value` | `context.portfolio.portfolio_value` |

### 4.3 自定义回调函数

使用 `小写下划线` 命名：

```python
# ✅ 正确
def interval_handle(context):
    pass

def get_finance(context):
    pass

# ❌ 错误
def intervalHandle(context):       # 驼峰命名
    pass

def MyCustomFunction(context):     # 大驼峰 / 不符合 PTrade 风格
    pass
```

---

## 5. API 使用规范

### 5.1 设置函数

必须在 `initialize()` 中调用，**使用命名参数传参**，禁止位置参数。

```python
# ✅ 正确：使用命名参数
set_commission(commission_ratio=0.0003, min_commission=5.0)
set_slippage(slippage=0.002)

# ❌ 错误：使用位置参数（顺序不明确，容易出错）
set_commission(0.0003, 5.0)
set_slippage(0.002)
```

### 5.2 数据获取

优先使用 `get_history()` 而非 `get_price()`。获取数据后**必须检查返回值**。

```python
# ✅ 正确：检查 None 和长度
df = get_history(10, '1d', 'close', security, fq=None, include=False)
if df is None or len(df) < 10:
    return

# ❌ 错误：未检查返回值，可能引发 AttributeError
df = get_history(10, '1d', 'close', security)
ma5 = df['close'][-5:].mean()  # 如果 df 为 None 则崩溃
```

### 5.3 交易下单

```python
# ✅ 常用下单函数
order(security, 1000)               # 按数量买入（正数=买，负数=卖）
order_value(security, cash)         # 按金额买入
order_target(security, 500)         # 调仓到目标数量
order_target_value(security, value) # 调仓到目标金额
order_market(security, 1000)        # 市价下单

# ❌ 错误：不在清单中的函数
buy(security, 1000)                 # 不存在
market_order(security, 1000)        # 不存在
```

### 5.4 日志输出

**必须使用 `%s` 占位符格式**，禁止字符串拼接。

```python
# ✅ 正确：使用 %s 占位符
log.info("买入 %s, 价格 %.2f, 数量 %.0f股" % (security, price, amount))
log.info("MA%d=%.3f" % (g.ma_short, ma_short))

# ❌ 错误：直接拼接字符串
log.info("买入 " + security)
log.info(f"买入 {security}, 价格 {price}")
```

### 5.5 常用持仓与账户 API

```python
# 获取单只标的持仓 → 返回 Position 对象
position = get_position(security)
# Position 属性: amount, enable_amount, cost_basis, last_sale_price

# 获取多只标的持仓 → 返回 dict
positions = get_positions(security_list)

# 获取未完成订单
orders = get_open_orders()

# 撤单
cancel_order(order_id)
```

---

## 6. 代码风格规范

### 6.1 段落分隔

使用简洁的 `#` 注释标注代码段落：

```python
def handle_data(context, data):
    # 获取数据
    df = get_history(10, '1d', 'close', security, fq=None, include=False)

    # 计算指标
    close_series = df['close']
    ma_short = round(close_series[-5:].mean(), 3)
    ma_long = round(close_series[-20:].mean(), 3)

    # 交易逻辑
    if ma_short > ma_long:
        order_value(security, cash)
```

> **提示**：避免在 `handle_data` 中内联大量复杂逻辑，将其抽取为独立函数（如 `create_stocks()`、`get_weights()`），保持主流程简洁。

### 6.2 参数集中原则

**所有可调参数集中在 `initialize()` 中**通过 `g` 对象声明，或抽取到独立设置函数中：

```python
def initialize(context):
    # 设置股票池
    g.security = '600570.SS'

    # 自定义参数
    g.ma_short = 5
    g.ma_long = 20
    g.take_profit = 0.10

    # 回测配置
    set_benchmark('000300.XBHS')
    set_commission(commission_ratio=0.0003, min_commission=5.0)
    set_slippage(slippage=0.002)
```

对参数较多的策略，鼓励将设置逻辑抽取为独立函数：

```python
def initialize(context):
    set_params()
    set_variables()
    set_backtest()
```

### 6.3 注释规范

- 使用**中文注释**
- 注释写在代码**上方**（而非行尾）
- 关键逻辑用 `#` 开头注释说明

### 6.4 通用原则

- 不使用全局变量（除 `g` 对象外）
- 不在函数中硬编码魔法数字，统一提取到 `initialize()` 的 `g` 参数中
- 函数保持单一职责，合理拆分较长逻辑

---

## 7. 股票代码规范

### 7.1 后缀规则

| 品种 | 后缀 |
|------|------|
| 上海市场证券 | `.SS` |
| 深圳市场证券 | `.SZ` |
| 指数 | `.XBHS` |

```python
# ✅ 正确
g.security = '600570.SS'          # 上海
g.security = '000001.SZ'          # 深圳
set_benchmark('000300.XBHS')      # 指数

# ❌ 错误：缺少后缀
g.security = '600570'
g.security = '600570.XSHG'        # 尾缀应使用简称 .SS，非全称 .XSHG
```

### 7.2 股票池

```python
# 单只股票
g.security = '600570.SS'
set_universe(g.security)

# 多只股票（list）
g.security = ['600570.SS', '000001.SZ', '000858.SZ']
set_universe(g.security)
```

---

## 8. 数据结构速查

### 8.1 `g` — 全局对象

存储策略参数和状态变量的全局对象，可在所有函数中读写。

```python
g.security = '600570.SS'
g.ma_short = 5
g.entry_price = None
```

### 8.2 `Context` — 上下文对象（`context`）

| 属性 | 含义 |
|------|------|
| `context.blotter.current_dt` | 当前时间（`datetime.datetime` 对象） |
| `context.portfolio.cash` | 可用资金 |
| `context.portfolio.portfolio_value` | 总资产 |
| `context.portfolio.positions_value` | 持仓市值 |
| `context.previous_date` | 前一交易日 |
| `context.capital_base` | 起始资金 |

### 8.3 `BarData` — K 线数据对象（`data[security]`）

| 属性 | 含义 | 可用周期 |
|------|------|---------|
| `.open` | 开盘价 | 全部 |
| `.close` | 收盘价 | 全部 |
| `.price` | 最新价 | 全部 |
| `.high` | 最高价 | 全部 |
| `.low` | 最低价 | 全部 |
| `.volume` | 成交量 | 全部 |
| `.money` | 成交额 | 全部 |
| `.preclose` | 昨收盘价 | 仅日线 |
| `.high_limit` | 涨停价 | 仅日线 |
| `.low_limit` | 跌停价 | 仅日线 |
| `.is_open` | 停牌标志（0=停牌，1=非停牌） | 全部 |

### 8.4 `Portfolio` — 资产对象

```python
context.portfolio.cash            # 可用资金
context.portfolio.portfolio_value # 总资产
context.portfolio.positions_value # 持仓市值
context.portfolio.returns         # 收益比例
context.portfolio.pnl             # 浮动盈亏
```

### 8.5 `Position` — 持仓对象

```python
position = get_position(security)
position.amount          # 总持仓数量
position.enable_amount   # 可用数量（可卖）
position.cost_basis      # 持仓成本价格
position.last_sale_price # 最新价格
position.today_amount    # 今日开仓数量
```

### 8.6 `get_history()` 返回值

```python
# 单股票 → pandas.DataFrame（行索引=datetime，列=行情字段）
df = get_history(10, '1d', 'close', security, fq=None, include=False)
close_series = df['close']

# 多股票 → pandas.DataFrame（行索引=datetime，列=[code, 行情字段]）
df = get_history(10, '1d', ['close', 'volume'], security_list=[...])
```

---

## 9. 规范核查清单

每个策略完成编写后，按以下 6 项逐条自查：

### 9.1 设置函数参数

- [ ] 所有设置函数（`set_commission`、`set_slippage` 等）使用了**命名参数**而非位置参数
- [ ] 参数名称、类型、顺序与 PTrade API 文档一致

### 9.2 数据获取函数参数

- [ ] `get_history()` 等在回测模块可用的函数，参数用法与文档定义一致
- [ ] `fq`、`include`、`security_list` 等选填参数正确传参
- [ ] 优先使用 `get_history()` 而非 `get_price()`

### 9.3 数据结构属性

- [ ] 所有对象属性（`context.portfolio.cash`、`position.amount` 等）拼写正确
- [ ] 未使用不存在的属性（如 `context.portfolio.cash_value`）

### 9.4 股票代码尾缀

- [ ] 上海市场使用 `.SS`（非 `.XSHG`）
- [ ] 深圳市场使用 `.SZ`（非 `.XSHE`）
- [ ] 指数使用 `.XBHS`
- [ ] 所有代码均带后缀，无遗漏

### 9.5 日志格式

- [ ] `log.info()` 使用 `%s` 占位符格式
- [ ] 未使用字符串拼接（`+`）或 f-string（`f"..."`）
- [ ] 如有选股逻辑，通过 `log.info` 输出选股结果

### 9.6 数据类型

- [ ] `get_history()` 返回 DataFrame，使用 `df['close']` 等方式取值
- [ ] `get_position()` 返回 Position 对象，通过 `.amount` 等属性访问
- [ ] 数据获取后检查了 `None` 和长度不足的情况

---

## 附录：PTrade API 参考文档

### 在线文档

| 链接 | 内容 |
|------|------|
| [PTrade API 文档](https://ptradeapi.com/) | PTrade 全部 API 接口官方文档，日常开发优先查阅 |

### 本地文档

详细 API 参数说明请查阅 `docs/ptrade/`：

| 文件 | 内容 |
|------|------|
| `1、PTrade策略开发提示词（参考模板）.md` | 策略开发全流程提示词模板 |
| `2、PTrade所有API函数接口清单.md` | 所有可用 API 函数清单 |
| `3、API接口明细-设置函数.json` | 设置函数完整参数说明 |
| `4、API接口明细-获取信息函数.json` | 获取信息函数完整参数说明 |
| `5、PTrade数据结构.json` | g/Context/BarData/Portfolio/Position/Order 等完整数据结构 |
| `6、Ptrade API 完整文档.md` | ptradeapi.com 完整 API 文档（首页全量内容，AI 可直接读取） |

---

## 附录：常见错误速查

| 错误 | 原因 | 修正 |
|------|------|------|
| `AttributeError: 'Portfolio' object has no attribute 'cash_value'` | 属性名写错 | `context.portfolio.cash` |
| `NameError: name 'get_price' is not defined` | 应使用 `get_history` | 替换为 `get_history()` |
| `log.info("buy " + security)` | 直接拼接字符串 | `log.info("buy %s" % (security))` |
| `g.security = '600570'` | 缺少代码后缀 | `g.security = '600570.SS'` |
| `set_commission(0.0003, 5.0)` | 使用位置参数 | `set_commission(commission_ratio=0.0003, min_commission=5.0)` |
